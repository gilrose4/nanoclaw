# Intent: Add LangSmith env vars to .env.example

Append LangSmith tracing configuration variables to `.env.example`:
`LANGSMITH_TRACING`, `LANGSMITH_API_KEY`, `LANGSMITH_PROJECT`, `LANGSMITH_ENDPOINT`.

This is an append-only change. Existing entries must be preserved.
